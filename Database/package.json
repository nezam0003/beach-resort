{
  "name": "database",
  "version": "1.0.0",
  "description": "all data abour resort",
  "main": "index.js",
  "scripts": {
    "json:server": "json-server --watch data.json --port 8000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
